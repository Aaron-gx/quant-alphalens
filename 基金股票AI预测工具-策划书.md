# 基金·股票 AI 预测工具 —— 项目策划书

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-22 | 初版策划，基于 xystock 与 A-Great-Fund-Valuation-System 两个参考项目调研 |

---

## 1. 项目概述

### 1.1 背景

个人投资者面对的问题不是缺数据，而是**数据看不过来、指标读不懂、结论不敢信**。券商 APP 给的买卖点没有推理过程；通用 AI 工具每次分析要 15~20 分钟且不缓存。本项目目标是做一个**自己的、可验证的、越用越准的** AI 预测工具。

### 1.2 目标

1. 对 **A 股个股、场内 ETF/LOF、场外基金、港股通标的、主要指数** 给出未来涨跌幅的**概率化预测**（次日 / 1 周 / 1 月 / 3~6 月四个时间尺度）。
2. 提供**历史走势复盘 + 情景推演**（牛市/基准/熊市三情景，附触发条件）。
3. 内置**模拟盘**：预测跟踪验证（每日预测 vs 实际，自动算准确率）+ 虚拟账户（按建议模拟买卖，跟踪净值、胜率、回撤）。
4. 预测结果**回流校准**：每一次预测都落库，到期自动对账，形成"预测 → 验证 → 校准"的闭环，为后续微调/训练积累私有数据集。
5. 架构上**核心引擎与 UI 解耦**，后期部署到服务器，以 API 形式对接聊天机器人。

### 1.3 差异化价值

| 现有方案 | 本项目 |
|----------|--------|
| 券商 APP 给结论不给过程 | 分层推理链全程可见，每个结论可追溯到数据和新闻 |
| 通用 AI 工具慢、不缓存 | 多级缓存 + 增量更新，单次分析控制在 1~2 分钟 |
| 预测完就没人管 | 预测落库、到期自动对账、准确率报表，错了知道错在哪 |
| 千人一面的建议 | 用户画像（风险偏好/持仓/常犯错误）注入每次分析 |

---

## 2. 参考项目调研结论

调研对象：`E:\Learn\MeProject\2026.9.22-jijinyvce` 下的两个开源项目。

### 2.1 xystock（股票分析工具，重点参考）

**整体思路**：数据采集 → LLM 分层分析 → 综合预测，本地缓存降本提速。

**数据采集层**（全部走 akshare / efinance，免费）：

| 类别 | 接口/来源 |
|------|-----------|
| 指数实时行情 | `ak.stock_zh_index_spot_em('沪深重要指数')` |
| 指数估值 | `ak.stock_zh_index_value_csindex`（沪深300/中证500/中证1000/上证50/科创50 等 PE、股息率） |
| 市场活跃度 | `ak.stock_market_activity_legu`（乐咕乐股：涨跌平家数、涨停跌停数） |
| 涨跌停池 | `ak.stock_zt_pool_em` / `ak.stock_zt_pool_dtgc_em`（含涨停原因分布） |
| 资金流向 | `ak.stock_market_fund_flow`（主力/超大单/大单净流入） |
| 融资融券 | `ak.macro_china_market_margin_sh` + `_sz`（含周变化计算） |
| 宏观货币 | `ak.macro_china_money_supply`（M1/M2 及同比） |
| K线/技术指标 | efinance 为主源，自研 `kline_data_manager` 缓存层，计算 MA/MACD/RSI/KDJ/布林等 |
| 个股信息 | `ak.stock_financial_abstract`（财务摘要）、`ak.stock_dividend_cninfo`（分红）、筹码分布、ETF 持仓 |

**新闻收集方式（本项目重点借鉴）**：

- **个股新闻**：`ak.stock_news_em(stock_code)` —— 东方财富个股新闻，按发布时间排序，**只保留最近 N 天、最多 10 条**，避免撑爆 prompt。
- **宏观/政策新闻**：`ak.stock_news_main_cx()` —— 财新网宏观快讯，过滤出有摘要和链接的条目，统一字段格式（标题/内容/发布时间/链接）。
- **新闻情绪量化**：逐条调 LLM（JSON 模式），输出三维度打分：`sentiment`（乐观/中性/悲观）、`intensity`（强度 1-5）、`deviation`（预期偏离度 1-5）。这样新闻从"文本"变成可排序、可汇总的结构化信号。
- **容错设计**：每个数据源独立 try/except + 多级备用方案（活跃度失败 → 概念板块汇总 → 资金流向推断），单源失败不拖垮整体。

**AI 分析层**：分层流水线，每个子分析独立生成、独立缓存，最后由"综合分析师"汇总：

```
技术面分析 → 公司/ETF 分析 → 新闻面分析 → 筹码分析 → 基本面分析
        ↓ 全部作为摘要输入
   综合分析（资深投资顾问人设）→ 输出涨跌预测概率 + 操作建议 + 风险提示
```

- 预测输出格式固定：超短期(1-3天)/短期(1-3月)/中期(3-6月)的涨跌概率和幅度区间。
- 用户画像注入：风险偏好（保守/中性/激进/自定义原则）、用户观点、当前持仓、常犯错误。
- `temperature=0.3` 保证输出稳定；请求内容落盘到 `data/cache/req_*.txt` 便于复盘。

**回测模块**：`SimpleBacktest` 引擎——逐 bar 调策略函数返回 buy/sell/hold + 仓位比例，统计总收益/年化/最大回撤/夏普/胜率。

### 2.2 A-Great-Fund-Valuation-System（基金估值系统）

**可借鉴点**：

- **基金数据**：`ak.fund_name_em`（全量基金名录）、`ak.fund_open_fund_info_em`（单位净值走势/估算走势）、`ak.fund_value_estimation_em`（盘中实时估值）、`ak.fund_individual_basic_info_xq`（雪球基金详情）。
- **实时行情直连**：新浪 `hq.sinajs.cn`（股票/指数秒级行情，需带 Referer 头）、东财 `push2.eastmoney.com`（分时、K线 API）、天天基金 `fundgz.1234567.com.cn`（场外基金盘中估值）。
- **快讯新闻**：`ak.stock_info_global_em`（东财全球 7×24 快讯）+ `ak.stock_info_global_cls`（财联社），标题拼百度链接直达原文。
- **本地存储**：SQLite 存持仓、定投计划、API Key，纯本地不上云。
- **持仓管理**：4 位小数成本精度、定投回测（按周/按月）、资产曲线。

### 2.3 对本项目的启示

| 直接复用 | 需要改进 |
|----------|----------|
| akshare 数据源矩阵、多级容错、K线缓存 | 新闻源偏少（只有东财+财新），需加公告、研报、互动易、社交媒体热度 |
| 分层分析流水线、用户画像注入 | 预测没有结构化落库——无法验证准确率，本项目必须把预测存成结构化记录 |
| SimpleBacktest 回测框架 | 回测只支持规则策略，需扩展支持"AI 信号"回测和模拟盘复用同一引擎 |
| Streamlit UI | UI 与引擎耦合太紧，本项目要拆出 API 层供聊天机器人调用 |
| 单 LLM 分析 | 升级为 LLM + 量化模型双引擎交叉验证 |

---

## 3. 产品定位

- **目标用户**：A 股/基金个人投资者（先从自己用起），有港股通权限。
- **产品形态**：本地/服务器部署的预测分析平台 + 聊天机器人入口。
- **一句话定位**：一个**会对自己的预测负责**的 AI 投资分析引擎——每次预测都留痕、到期自动对账、准确率公开可查。

---

## 4. 功能设计

### 4.1 功能全景

```
┌─────────────────────────────────────────────────────────┐
│  接入层：Web 界面 │ REST API │ 聊天机器人 │ 定时任务推送     │
├─────────────────────────────────────────────────────────┤
│  应用层：分析报告 │ 涨跌预测 │ 情景推演 │ 模拟盘 │ 验证报表  │
├─────────────────────────────────────────────────────────┤
│  预测引擎：LLM 分层分析流水线 ＋ 量化模型 ＋ 结果融合校准    │
├─────────────────────────────────────────────────────────┤
│  数据层：行情 │ 新闻情报 │ 基本面 │ 宏观 │ 用户画像          │
├─────────────────────────────────────────────────────────┤
│  基础设施：多级缓存 │ SQLite/PostgreSQL │ 调度器 │ 日志     │
└─────────────────────────────────────────────────────────┘
```

### 4.2 M1：数据采集与情报中心

**行情数据**（复用参考项目方案）：
- 指数/个股/ETF 实时行情、K 线（日/周/月/分时）、五档盘口
- 技术指标自动计算：MA/MACD/RSI/KDJ/布林/量比/换手率
- 港股通标的行情（akshare `stock_hk_*` 系列接口）

**基本面数据**：
- 个股：财务摘要、盈利/偿债/成长指标、股息分红、筹码分布、股东户数
- 基金：净值走势、盘中估值、持仓明细（ETF 用 `etf_holdings`、场外基金用季报持仓）、基金经理、费率
- 指数：PE/PB/股息率及历史分位

**市场宏观数据**：
- 市场活跃度（涨跌家数/涨停跌停）、资金流向、融资融券余额及周变化、M1/M2、北向资金

**新闻情报子系统（提高预测准确率的关键，重点设计）**：

| 情报源 | 接口/方式 | 用途 |
|--------|-----------|------|
| 个股新闻 | `ak.stock_news_em` | 公司动态、舆情 |
| 宏观政策 | `ak.stock_news_main_cx`（财新） | 政策面、大盘 |
| 7×24 快讯 | `ak.stock_info_global_em` + `stock_info_global_cls`（财联社） | 突发事件盘中捕捉 |
| 公司公告 | `ak.stock_notice_report` / 巨潮 | 业绩、增减持、重组 |
| 研报评级 | `ak.stock_research_report_em` | 机构观点、评级变化 |
| 互动问答 | 互动易/e互动（akshare 有对应接口） | 公司一手回应 |
| 行业板块 | `ak.stock_board_*` | 板块轮动、概念热度 |
| 用户输入 | 聊天机器人/界面手动录入 | 微信群舆情等 AI 抓不到的信息 |

**新闻处理流水线**（比参考项目多两步）：

```
抓取 → 去重（标题相似度）→ 时效过滤 → 相关性判定（这条新闻和该标的有关吗）
     → LLM 情绪打分（sentiment/intensity/deviation，JSON 模式）
     → 聚合：情绪得分曲线、关键事件时间轴、利好/利空清单
     → 存入情报库（带标签，供预测引擎和复盘检索）
```

新增的两个关键改进：
1. **事件时间轴**：把新闻、公告、研报按时间挂到 K 线上，复盘"什么事件导致了什么走势"，也喂给 LLM 做归因。
2. **情报库落库**：新闻情绪打分结果全部存数据库，既供当下分析，也积累成训练/微调语料。

### 4.3 M2：预测引擎（核心）

采用 **"LLM 分层分析 + 量化模型" 双引擎 + 融合校准** 架构。

**引擎 A：LLM 分层分析流水线**（在 xystock 基础上扩展）

```
第 1 层 · 专项分析（并行、可缓存）：
  ├─ 技术面分析   （K线形态、指标、关键位）
  ├─ 基本面分析   （财务、估值、成长性 / 基金持仓与经理）
  ├─ 新闻面分析   （情报库近 N 天打分汇总 + 重点新闻解读）
  ├─ 筹码/资金面  （筹码分布、主力行为、北向/融资融券）
  └─ 市场环境分析 （大盘趋势、板块轮动、宏观流动性）

第 2 层 · 情景推演：
  基于第 1 层输出，生成三个情景：
  ├─ 乐观情景：概率、目标区间、触发条件
  ├─ 基准情景：概率、目标区间、触发条件
  └─ 悲观情景：概率、目标区间、触发条件

第 3 层 · 综合裁决（投资顾问人设 + 用户画像）：
  输出结构化预测 JSON：
  {
    "horizons": {
      "next_day":  {"up": 0.55, "flat": 0.25, "down": 0.20, "range": "±1.5%"},
      "one_week":  {...}, "one_month": {...}, "quarter": {...}
    },
    "scenarios": {...},
    "confidence": 0-100,
    "key_factors": ["..."],
    "action": {"advice": "...", "entry": ..., "stop_loss": ..., "target": ...},
    "risks": ["..."]
  }
```

- 固定 JSON Schema 输出是**验证闭环的前提**——概率和区间必须机器可读，不能只是散文。
- temperature 0.2~0.3；每层输出落盘可复盘。

**引擎 B：量化模型**（你有时间训练，分阶段上）

- Phase 2 先上**轻量基线模型**：LightGBM / 逻辑回归，特征用技术指标 + 情绪得分 + 资金流 + 估值分位，预测次日/5 日涨跌方向概率。作用不是取代 LLM，而是提供一个**不带叙事偏见的独立信号**。
- Phase 3 可选：时序模型（ Prophet / NeuralForecast 系列）做价格区间预测。
- 训练数据 = 历史行情特征 + 情报库情绪得分 + 已验证的预测记录。

**融合层**：

- 双引擎输出不一致时**降置信度并提示分歧原因**（"技术面看空但新闻面强利好"），这比强行给单一结论更诚实。
- 校准：用预测验证库的历史命中率对各引擎动态加权（如 LLM 近 30 日次日方向命中率 58%、量化模型 54%，则权重相应倾斜）。

### 4.4 M3：历史走势与情景推演

- **走势复盘**：K 线叠加事件时间轴（新闻/公告/研报/分红/龙虎榜），标注每次 AI 预测的点位，一目了然"当时 AI 怎么判的、后来怎么走"。
- **归因分析**：对显著异动（单日 ±5%）自动触发"为什么涨/跌"分析——拉取当日新闻、资金流、板块联动，LLM 生成归因报告。
- **情景推演**：基于当前数据模拟三个情景下的路径（结合 M2 第 2 层），给出各情景的观察指标和应对预案（"若跌破 X 且成交量放大 → 悲观情景概率上修"）。
- **相似历史匹配**（量化侧）：用 DTW/相关性在当前标的和指数历史中找相似走势片段，展示"历史上类似形态后来怎么走的"。

### 4.5 M4：预测验证系统（准确率闭环）

这是本项目区别于一般 AI 分析工具的核心：

1. **预测落库**：每次预测以结构化记录存 `predictions` 表（标的、时间、horizon、概率分布、区间、置信度、引擎版本、当时输入数据快照哈希）。
2. **到期自动对账**：调度器每日跑批，到期预测自动对比实际走势，计算方向是否命中、区间是否覆盖、概率校准误差（Brier score / 校准曲线）。
3. **准确率看板**：按标的类型/horizon/引擎/置信度分桶统计命中率、平均误差、最近趋势，公开透明。
4. **失败案例库**：预测错得离谱的案例自动标记，进入复盘队列，供人工+AI 分析"错在哪"——是数据缺失、事件突发还是推理失误，反哺 prompt 和特征工程。
5. 该库同时就是**未来微调/训练的标注数据集**。

### 4.6 M5：模拟盘

**A. 预测跟踪**（上述 M4 的对外展示面）：每日对关注标的发布预测，类似参考项目公众号每日预测上证指数的做法。

**B. 虚拟账户**：
- 多账户支持（如"保守组合""激进组合"各一个），初始虚拟资金可配。
- 交易规则贴近实盘：T+1、涨跌停限制、手续费/印花税、基金申购赎回费率、港股通汇率。
- 两种驱动模式：**手动模式**（用户自己下单，验证自己判断）和 **AI 信号模式**（按预测引擎建议自动调仓，验证 AI）。
- 组合分析：净值曲线、收益率、最大回撤、夏普、胜率、持仓分布、与沪深300 基准对比。
- 复用回测引擎：同一套撮合/记账内核，回测跑历史、模拟盘跑当下，保证口径一致。

### 4.7 M6：用户画像与个性化

沿用 xystock 思路并扩展：风险偏好（保守/中性/激进/自定义原则）、当前持仓、交易风格（左/右侧、长/短线）、自述常犯错误、可投资产与仓位约束。每次分析作为 system context 注入，建议因人而异。

### 4.8 M7：报告与推送

- 分析报告导出 Markdown / PDF。
- **定时任务**：盘前（政策面+隔夜外盘简报）、盘后（当日复盘+次日预测）、盘中异动推送。
- **聊天机器人接口**：REST API 供机器人调用——`/predict`（查预测）、`/analyze`（发起分析）、`/portfolio`（查模拟盘）、`/alert`（推送）。用户也可通过机器人输入"微信群在传 XX"这类人工情报直接进入情报库。

---

## 5. 技术架构

### 5.1 总体架构

```
┌──────────────┐  ┌──────────────┐  ┌───────────────┐
│  Streamlit    │  │  聊天机器人    │  │  定时任务       │
│  管理界面      │  │  (微信/TG等)  │  │  (盘前/盘后)    │
└──────┬───────┘  └──────┬───────┘  └──────┬────────┘
       │                 │                 │
       └─────────────────┼─────────────────┘
                         ▼
              ┌─────────────────────┐
              │   FastAPI 服务层     │  ← REST API，鉴权
              └──────────┬──────────┘
                         ▼
        ┌────────────────────────────────┐
        │        核心引擎（纯 Python 包）    │
        │  ┌──────────┐  ┌─────────────┐ │
        │  │ 数据层    │  │ 情报流水线   │ │
        │  │ fetchers │  │ 去重/打分/入库│ │
        │  ├──────────┤  ├─────────────┤ │
        │  │ LLM流水线│  │ 量化模型     │ │
        │  ├──────────┤  ├─────────────┤ │
        │  │ 融合校准  │  │ 验证对账     │ │
        │  ├──────────┤  ├─────────────┤ │
        │  │ 模拟盘引擎│  │ 回测引擎     │ │
        │  └──────────┘  └─────────────┘ │
        └───────────────┬────────────────┘
                        ▼
        ┌───────────────────────────────┐
        │ SQLite → PostgreSQL │ 文件缓存  │
        └───────────────────────────────┘
```

**关键设计原则**：核心引擎不依赖任何 UI/框架，FastAPI、Streamlit、机器人都是它的"客户端"。

### 5.2 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 语言 | Python 3.11+ | 参考项目同栈，akshare 生态 |
| 数据源 | akshare + efinance + 新浪/东财直连 | 免费，覆盖 A股/基金/港股通 |
| LLM | OpenAI 兼容协议，默认 DeepSeek（chat + reasoner） | 便宜、懂中国语境；接口兼容可随时换 GPT/Qwen |
| 服务层 | FastAPI | 异步、自动生成 OpenAPI 文档，机器人好对接 |
| 界面 | 一期 Streamlit，二期按需换 Vue/React | 先快后美；引擎解耦后换 UI 无痛 |
| 数据库 | SQLite（本地期）→ PostgreSQL（服务器期） | SQLAlchemy 抽象，切换只改连接串 |
| 量化 | pandas / numpy / LightGBM / scikit-learn | 基线模型够用 |
| 图表 | plotly | 交互式 K线、净值曲线 |
| 调度 | APScheduler（容器内） | 盘前盘后批处理、预测对账 |
| 部署 | Docker Compose | 与参考项目一致，服务器一把梭 |

### 5.3 核心数据表（概要）

```
predictions       预测记录（标的/horizon/概率JSON/置信度/引擎/输入快照）
prediction_evals  到期评估（命中与否/误差/Brier score）
news_items        情报库（原文/情绪打分/关联标的/事件标签）
signals           双引擎输出明细（融合前各自结论）
paper_accounts    虚拟账户
paper_orders      模拟委托/成交
positions         虚拟持仓
nav_history       账户净值历史
watchlist         自选标的
user_profile      用户画像与偏好
analysis_reports  分析报告存档（含prompt快照，可复盘）
```

### 5.4 部署形态

- **开发期**：本地跑，`streamlit run` + `uvicorn` 双进程，或先只做引擎+Streamlit。
- **服务器期**：`docker-compose` 一键起 `api` + `web` + `scheduler` + `db` 四容器；机器人走公网调 API（API Key 鉴权 + HTTPS）。

---

## 6. 预测准确性提升策略（重点）

准确性是本项目的立身之本，手段按优先级排序：

1. **情报质量 > 模型聪明度**。新闻是最主要增量信息，按 §4.2 的多源情报矩阵 + 情绪结构化打分执行；特别注意"预期偏离度"维度——符合预期的利好不涨反跌往往更重要。
2. **预测必须结构化**。散文预测无法验证；固定 JSON Schema + 概率分布是闭环的前提。
3. **双引擎交叉验证**。LLM 擅长叙事与突发事件，量化模型擅长统计规律；分歧本身就是信号。
4. **验证闭环**。每日自动对账 → 校准曲线 → 动态调权 → 失败案例反哺 prompt/特征。
5. **可解释性**。每个预测带推理链和数据快照，错了能定位是数据问题还是推理问题。
6. **诚实机制**。数据缺失/信源冲突时降置信度并明说，宁可说"看不清"不硬给结论。
7. **长期：私有数据集**。predictions + news_items + evals 积累 3~6 个月后，就是微调模型或训练校准器的高质量语料——这是"越用越准"的物质基础。

**必须诚实声明**：短期价格预测本质上是低信噪比问题，学术和业界共识是次日方向准确率能稳定超过 55% 已属优秀。本工具的目标是**概率化表达 + 持续校准 + 完整推理链**，而不是承诺"必涨"。策划书中所有预测功能均以此定位。

---

## 7. 开发计划

| 阶段 | 周期(估) | 交付物 |
|------|-----------|--------|
| **P0 地基** | 1~2 周 | 项目骨架、配置管理、数据库、缓存层、行情 fetcher（复用参考项目代码思路）、LLM 客户端 |
| **P1 最小闭环** | 2~3 周 | 情报子系统（东财+财新+财联社）、LLM 分层流水线、结构化预测输出、predictions 落库、Streamlit 基础界面（看行情/看分析/看预测） |
| **P2 验证+模拟盘** | 2~3 周 | 自动对账调度、准确率看板、虚拟账户（手动+AI 信号两种模式）、净值曲线、报告导出 |
| **P3 服务化** | 1~2 周 | FastAPI 层、鉴权、docker-compose、机器人接口、定时推送 |
| **P4 变准** | 持续 | 量化基线模型、融合校准、失败案例库复盘、情景推演完善、港股通打磨 |

P0~P3 预计 **6~10 周**出可用完整版（个人项目节奏）。

---

## 8. 成本估算

| 项 | 估算 |
|----|------|
| 行情/新闻数据 | 0 元（akshare/东财/新浪免费接口） |
| LLM（DeepSeek） | 单次完整分析约 3~8 万 token，按 deepseek-chat 计约 ¥0.1~0.5/次；每日 10 标的 + 盘中异动 ≈ ¥5/天 ≈ **¥150/月**量级，用 reasoner 翻倍 |
| 服务器 | 轻量云主机即可，¥50~100/月 |
| 合计 | **¥200~300/月** 量级 |

降本手段：子分析结果缓存、同一标的日内复用数据快照、非交易时段降频、情绪打分用小模型。

---

## 9. 风险与合规

| 风险 | 应对 |
|------|------|
| **合规风险**：对外提供投资建议需持牌 | 定位个人自用工具；若机器人对外，输出统一带"仅供参考，不构成投资建议"免责声明，不做荐股收费 |
| 数据源接口变动/限流 | 每源多级备用 + 缓存 + 失败降级（参考 xystock 容错设计）；请求限速 |
| LLM 幻觉 | 只允许基于注入数据分析，关键数字由代码计算注入而非让模型背；输出校验 |
| 预测不准损害信任 | 准确率看板公开透明，错就是错；置信度机制；强调概率化而非确定性 |
| API Key/持仓隐私 | 本地/自有服务器存储，敏感配置加密，日志脱敏 |

---

## 10. 附录

### 10.1 建议目录结构

```
2026.9.22-JiJin/
├── core/                  # 核心引擎（UI 无关）
│   ├── data/              #   行情/基本面 fetchers
│   ├── intel/             #   新闻情报流水线
│   ├── predict/           #   LLM 流水线 + 量化模型 + 融合
│   ├── verify/            #   预测对账与校准
│   ├── paper/             #   模拟盘 + 回测引擎
│   └── store/             #   数据库模型与缓存
├── api/                   # FastAPI 服务层
├── ui/                    # Streamlit 界面
├── bots/                  # 聊天机器人适配
├── jobs/                  # 定时任务（盘前/盘后/对账）
├── tests/
├── docker-compose.yml
└── requirements.txt
```

### 10.2 参考项目可复用清单

| 来源 | 可复用内容 |
|------|-----------|
| xystock `market/`、`stock/` | 全部 akshare 数据获取与容错模式 |
| xystock `utils/news_tools.py` | 新闻获取与情绪打分模板 |
| xystock `stock/stock_ai_analysis.py` | 分层分析 prompt 结构 |
| xystock `backtesting/` | 回测引擎骨架（扩展撮合规则） |
| xystock `stock/analysis_prompts.py` | 风险偏好 prompt |
| fund_assistant `data_api.py` | 基金估值、新浪/东财直连、快讯 |
| fund_assistant `database.py` | SQLite 持仓管理思路 |
